<h1><%= t('view.log in') %></h1>
<%= form_with(scope: :session, url: sessions_path, local: true) do |f| %>

  <%= f.label t('view.email') %>
  <%= f.email_field :email %>

  <%= f.label t('view.password')  %>
  <%= f.password_field :password %>

  <%= f.submit t('view.log in') %>
<% end %>